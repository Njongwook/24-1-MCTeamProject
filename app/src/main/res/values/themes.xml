<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TimeConversionApplication" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/white</item>
        <item name="toolbarStyle">@style/CustomToolBar</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="CustomToolBar" parent="@style/Widget.AppCompat.Toolbar">
        <item name="titleTextColor">@color/blue4</item>
    </style>
    <style name="Theme.TimeConversionApplication" parent="Base.Theme.TimeConversionApplication" />
</resources>